
  # Convert prototype to HTML

  This is a code bundle for Convert prototype to HTML. The original project is available at https://www.figma.com/design/zdWfjt30AmmiltUDrVBRiS/Convert-prototype-to-HTML.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  